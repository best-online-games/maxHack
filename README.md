# Добро.Рядом - Волонтерское приложение

Платформа для волонтеров, которая позволяет делать добрые дела за 10-30 минут и получать очки.

## 🚀 Технологический стек

- **Frontend**: Next.js 16.0, React 19.2, TypeScript
- **Backend**: Supabase (PostgreSQL + Authentication)
- **UI**: Radix UI, Tailwind CSS 4, Lucide Icons
- **Forms**: React Hook Form + Zod
- **Аналитика**: Vercel Analytics

## 📋 Возможности

- ✅ Регистрация и авторизация через email
- ✅ Система очков и монет за выполнение активностей
- ✅ Волонтерские задания с разными категориями
- ✅ Челленджи с наградами
- ✅ Флешмобы и локальные заявки на помощь
- ✅ Таблица лидеров
- ✅ Магазин подарков за монеты
- ✅ Донаты в фонды
- ✅ Профиль пользователя

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <url-репозитория>
cd max
```

### 2. Установка зависимостей

Проект использует `pnpm`, но можно использовать и `npm`:

```bash
# Если используете pnpm
pnpm install

# Если используете npm (с флагом из-за React 19)
npm install --legacy-peer-deps
```

### 3. Настройка Supabase

#### 3.1. Создание проекта в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь инициализации базы данных

#### 3.2. Настройка базы данных

Выполните SQL-скрипты в следующем порядке через SQL Editor в Supabase Dashboard:

1. `scripts/001_create_tables.sql` - создание всех таблиц
2. `scripts/002_seed_data.sql` - начальные данные
3. `scripts/003_create_functions.sql` - функции и триггеры (если есть)

#### 3.3. Получение API ключей

1. В Supabase Dashboard перейдите в `Settings` → `API`
2. Скопируйте:
   - `Project URL`
   - `anon public` ключ

#### 3.4. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

**⚠️ Важно**: Замените значения на ваши реальные credentials из Supabase!

### 4. Запуск проекта

#### Development режим

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

#### Production build

```bash
npm run build
npm run start
```

#### Lint

```bash
npm run lint
```

## 📁 Структура проекта

```
max/
├── app/                    # Next.js App Router
│   ├── auth/              # Страницы авторизации
│   ├── activities/        # Список активностей
│   ├── challenges/        # Челленджи
│   ├── leaderboard/       # Таблица лидеров
│   ├── profile/           # Профиль пользователя
│   ├── rewards/           # Магазин подарков
│   ├── flashmobs/         # Флешмобы
│   ├── donate/            # Донаты
│   └── layout.tsx         # Корневой layout
├── components/            # React компоненты
│   ├── ui/               # UI компоненты (Radix UI)
│   └── *.tsx             # Компоненты приложения
├── lib/                   # Утилиты и типы
│   ├── supabase/         # Supabase клиенты
│   ├── types.ts          # TypeScript типы
│   └── utils.ts          # Вспомогательные функции
├── scripts/              # SQL скрипты для БД
│   ├── 001_create_tables.sql
│   └── 002_seed_data.sql
├── public/               # Статические файлы
├── middleware.ts         # Next.js middleware (auth)
└── package.json
```

## 🗄️ База данных

### Основные таблицы:

- **profiles** - профили пользователей с монетами и очками
- **activities** - волонтерские активности
- **activity_categories** - категории активностей
- **user_activities** - участие пользователей в активностях
- **challenges** - челленджи
- **user_challenges** - прогресс по челленджам
- **rewards** - подарки в магазине
- **user_rewards** - купленные подарки
- **flashmobs** - флешмобы
- **help_requests** - локальные заявки на помощь
- **donations** - история донатов

### Row Level Security (RLS)

Все таблицы защищены политиками RLS:
- Пользователи видят только свои данные
- Публичные данные доступны всем
- Модификация только для владельцев записей

## 🔐 Аутентификация

Проект использует Supabase Auth с email/password:

- Регистрация: `/auth/signup`
- Вход: `/auth/login`
- Защита роутов через `middleware.ts`
- Автоматический редирект на логин для неавторизованных

## 🎨 Темизация

Проект использует Tailwind CSS 4 с кастомной темой:
- Градиенты от голубого к бирюзовому
- Адаптивный дизайн (mobile-first)
- Dark mode поддержка через `next-themes`

## 📱 Компоненты

### UI компоненты (Radix UI)
- Button, Card, Dialog, Dropdown
- Form components (Input, Label, Select)
- Toast notifications (Sonner)
- И многое другое

### Кастомные компоненты
- `HomeHeader` - шапка главной страницы
- `CategoryGrid` - сетка категорий
- `ActivityFeed` - лента активностей
- `BottomNav` - нижняя навигация
- `QuickActions` - быстрые действия

## 🚨 Известные проблемы

1. **React 19 конфликты**: Некоторые библиотеки еще не обновились под React 19. Решение: использовать `--legacy-peer-deps`
2. **TypeScript errors игнорируются**: В `next.config.mjs` включен `ignoreBuildErrors: true`

## 🔧 Troubleshooting

### Ошибка: "Invalid login credentials"
- Проверьте, что пользователь создан в Supabase Auth
- Убедитесь, что email подтвержден (или отключите подтверждение в настройках Supabase)

### Ошибка: "Failed to fetch"
- Проверьте `.env.local` файл
- Убедитесь, что NEXT_PUBLIC_SUPABASE_URL и KEY правильные
- Проверьте доступность Supabase проекта

### Проблемы с установкой
```bash
# Очистить кеш и переустановить
rm -rf node_modules package-lock.json
npm install --legacy-peer-deps
```

## 📝 TODO

- [ ] Добавить unit тесты
- [ ] Настроить CI/CD
- [ ] Добавить уведомления в реальном времени
- [ ] Геолокация для локальных заявок
- [ ] Push-уведомления
- [ ] PWA поддержка

## 🤝 Контрибьюция

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 👨‍💻 Автор

Сгенерировано с помощью v0.app и доработано вручную.
# maxHack
